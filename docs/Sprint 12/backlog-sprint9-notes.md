# Backlog — logged during Sprint 9 (2026-07-10/11), unscheduled

| # | Item | Category | Notes |
|---|---|---|---|
| 1 | Ribbon buttons in 3-row columns get cut off, and (confirmed 2026-07-13) ANY ribbon-embedded HTML content extending past the ribbon's small fixed height gets clipped — this is not ordinary page-CSS `overflow`, it's the ribbon's own Electron `WebContentsView` bounds (per ADR-021's `RIBBON_HEIGHT=124`) hard-clipping at the view-rectangle level | Architectural constraint, broader than originally understood | Originally logged Sprint 9 as FIGURES-specific, confirmed Sprint 11 as app-wide across 3-row columns, and confirmed Sprint 12 (2026-07-13) to also affect a custom HTML dropdown popup for the same root reason — anything rendered inside the ribbon's own WebContentsView is bounded by that view's small height, full stop. VS Code `QuickPick`s and Electron's native `Menu.popup()` both correctly escape this (they render outside the ribbon view entirely), which is now the established workaround pattern for any ribbon UI that needs more vertical space than the ribbon itself provides. Genuinely fixing the underlying height constraint (vs. routing around it) remains undesigned. |
| 2 | Plot Builder webview panel should auto-close on JuliaLab shutdown | Teardown/lifecycle | Currently no explicit disposal hook tied to app quit — relies on default VS Code panel disposal behavior, unverified whether that's clean or leaves state. Should be evaluated alongside existing teardown verification discipline (PowerShell process-diff technique) rather than assumed fixed by a small code change. |
| 3 | Command Palette (`Ctrl+Shift+P`) is high-friction for users coming from MATLAB — no lower-friction path to it currently | UX/discoverability | Options to consider later: a ribbon button, a persistent search-style affordance, or exposing frequently-needed commands directly on relevant ribbon tabs instead of requiring the palette at all. Genuinely open design question, not a quick win — flagging rather than guessing at a fix. |
| 4 | F2 (Hide Ribbon) reverts to standard VSCodium look — asking whether this is desired/reversible behavior | Behavior question, not clearly a bug | Sounds like F2 is working as designed (SPRINT9-HANDOFF.md lists "F2 / Hide Ribbon: ✓ Collapses to tab strip") — worth clarifying with John whether the actual complaint is that it *fully* reverts to standard VSCodium chrome rather than just collapsing to tab strip, which would be a scope difference from what Sprint 6-8 shipped. Needs clarification before it's actionable. |
| 5 | Activity bar icons are monochrome line drawings — request to colorize (example: Wolfbook icon, referenced against a colorful stylized wolf-head logo) | Visual/branding polish | Undesigned. Would need icon assets sourced/created per activity-bar entry (Pluto, Lean, Wolfram, Claude, Julia, etc.) — also adjacent to Task 009 (activity bar launchers), which is itself already flagged as not-yet-designed in tasks.md. Worth designing together rather than separately, when that task is picked up. |

| 6 | Plot Builder interaction model — variable selection resets every time the panel is reopened/revealed (re-clicking Plot after adjusting a ribbon option wipes X/Y dropdown selections), and John's broader critique: selecting plot type/style/axes on the ribbon THEN variables in a separate webview is clumsy — worth exploring "pick variables first from workspace, plot options after" or a single unified surface | UX/interaction design | Root mechanical cause of the reset identified during Task 007 testing: `openPlotBuilderPanel()` re-sends `init` and re-populates dropdowns from scratch on every Plot-button click, even when revealing an already-open panel, discarding in-progress selections. That's a real, fixable bug regardless of the larger design question. The larger question (should the two-context ribbon+webview model change entirely) needs real Socratic design work — not a quick patch — deferred to a future sprint per John's explicit request (2026-07-11) to prove Task 007's core execution path first. |

| 7 | Four Sprint 8 "✓ Wired" features found non-functional or worse during Sprint 9 regression testing (2026-07-11): New Folder (no effect), Go to Def ("No Definition Found"), Forward/Back (no action), and Instantiate (each invocation spawns a NEW Julia REPL terminal and disconnects the PREVIOUS one — not just accumulating broken REPLs, but actively severing the prior connection each time; only the most-recently-spawned REPL remains usable) | Pre-existing gap, confirmed NOT a Sprint 9 regression | `ws-dispatch-probe.txt` (Task 002's diagnostic, still active) confirmed `language-julia.instantiateEnvironment` reaches the extension host correctly with the right message shape — ruling out Sprint 9's ADR-024/main.js dispatch changes as the cause. John confirmed he doesn't recall these ever having been behaviorally verified in Sprint 6-8 — "✓ Wired" apparently meant "WS message sends," not "resulting behavior confirmed." The Instantiate REPL-replacement pattern is the highest-priority of the four — real process/state instability, not just an inert button — worth its own investigation in a future sprint, likely related to whatever manages the single-REPL assumption baked into `startJuliaRepl()`/session lifecycle. |

| 8 | FIGURES tab missing several MATLAB-parity features observed directly in MATLAB's FIGURE tab (2026-07-11 screenshot comparison): Subtitle, Legend/Colorbar as distinct labeled controls (Legend exists in current AXES group; Colorbar does not), X-Grid/Y-Grid as separate toggles (current AXES group only has a single combined "Grid" option), and a text-arrow annotation tool | Feature gap, MATLAB-parity | Not part of the interaction-model redesign (item #6) — this is additive scope (new plot-formatting capabilities), not a rework of the existing selection flow. Candidate for whichever future sprint tackles FIGURES tab expansion; needs its own SDD-level scoping (which of these are Plots.jl-supportable, which require StatsPlots or other packages) before design. |

| 9 | Multi-variable plotting (3+ selected variables) — two genuinely distinct, mutually incompatible use cases surfaced during Sprint 10 Task 006 testing: (a) multiple Y-series sharing one X (e.g. comparing several sine waves: `plot(θ, [y1 y2])`), and (b) 3D parametric paths (e.g. Lorenz attractor: `plot(x(t), y(t), z(t))`, not a surface/mesh). Neither is distinguishable from variable data alone — same "3 vectors selected" input either way. Proposed disambiguation (not yet built): use the existing PLOT TYPE ribbon selection to decide — Surface/Contour keeps current grid+matrix behavior; a new "3D Line"/Path3D plot type (doesn't exist yet, MATLAB has `plot3` as a distinct type) would produce the parametric-path case; any other plot type with 3+ selected would default to multi-series (first = shared X, rest = Y columns) | New feature, explicitly deferred | Raised during Sprint 10 Task 006 testing (2026-07-11) — John confirmed this is real, common MATLAB-user-expected functionality, not edge-case scope creep, but explicitly out of Sprint 10's scope (which was the 2-variable interaction-model redesign, not new plotting capabilities). Adjacent to backlog #8 (MATLAB-parity feature gaps) — worth designing together when that's picked up, since both are "what can the FIGURES tab actually plot" scope rather than "how does selection work" scope. |

| 10 | `StatsPlots.jl` is not installed in John's Julia environment (`Pkg.add("StatsPlots")` fails on an unrelated `Qt6Base_jll` version-resolution conflict, likely from multiple active registries — DyadRegistry/General/JuliaComputingRegistry). Confirmed to block Boxplot (Sprint 9 and re-confirmed Sprint 10) and Violin (Sprint 10) — any plot type routed through StatsPlots will show the same "moved to StatsPlots" warning and fail identically | Environment issue, not app code | Not a JuliaLabApp bug — `generatePlotCode()`'s `using StatsPlots` auto-prepend (Decision 1A) is confirmed working correctly every time this has been tested; the failure is entirely in package resolution on this machine. Fixing this is a Julia/Pkg registry cleanup task for John, unrelated to any Sprint 9/10 code. Consolidating here so it isn't re-diagnosed from scratch again. |

| 11 | Histogram plot's legend shows "y" even when the actual plotted variable is `θ` (data itself confirmed correct via REPL — `histogram(θ, ...)` genuinely plots θ's values; only the legend text is questionable) | Cosmetic, minor | Found during Sprint 10 Task 009 integration testing (2026-07-11). Two candidate explanations, neither confirmed: (a) Plots.jl's own default auto-label behavior for an unlabeled histogram series, unrelated to our code; (b) a stale legend artifact carried over from an earlier plot under the Reuse-Figure semantic (Sprint 10's explicit decision, SDD §5). If (b), this would be a real — if minor — side effect of Reuse-Figure worth knowing about, since it's adjacent to (but distinct from) the confirmed-working formatting-persistence behavior that's this sprint's actual deliverable. Not blocking Sprint 10 closure; worth a quick look in a future sprint. |

| 12 | FIGURES "New" button doesn't open a new blank plot window | Possibly obsolete, not a bug | Found during Sprint 10 Task 010 regression testing (2026-07-11). John's own assessment: this may simply be redundant now — Sprint 10's reactive model means plots appear automatically on variable selection, so a manual "create blank plot window" affordance may no longer fit the interaction model at all. Worth a deliberate decision (remove the button vs. give it new meaning) rather than treating as a bug to fix, in a future sprint. |
| 13 | FIGURES "Close All" works but leaves an empty "Julia Plots (0/0)" tab open rather than closing it | Minor UI residue | Found during Sprint 10 Task 010 regression testing (2026-07-11). Not investigated — likely closes plot content but not the tab container itself. Low priority, cosmetic. |

| 14 | GLMakie / interactive native-window plotting (drag-to-place text annotations, interactive 3D rotation), Text Arrow annotations (both drag and typed-coordinate versions), AND proper Title/Subtitle support — proposed as Sprint 11 additions, deferred together to a future initiative | New feature area, explicitly deferred, needs its own Socratic scoping | Raised 2026-07-11 with a working GLMakie drag-to-place code example. Research confirmed GLMakie isn't a small addition: its drag interactivity specifically requires the native-desktop-window variant (`GLMakie.activate!()`), not the VS Code-plot-pane-embedded variant (WGLMakie) — Makie's own community has had recurring confusion/bugs about which mode activates when. This means a second, incompatible plotting paradigm (Makie's `Figure`/`Axis`/`Observable` reactive model) alongside Plots.jl, a parallel code-generation engine, a UX decision about plots escaping the embedded pane, and new dependency risk on a machine that already has one unresolved package-registry conflict (backlog #10). John separately clarified (2026-07-11) that even a basic non-dragging typed-coordinate text-arrow version should wait and be designed together with GLMakie. Subtitle joined this deferred group (2026-07-11, Sprint 11 testing) after `plotattr("subtitle")` confirmed no such Plots.jl attribute exists — the real mechanism (`plot_title` for a main title + `title` for a smaller subtitle underneath, confirmed working via direct REPL test) needs a genuine Title+Subtitle pair design, not a single kwarg, and John suspects GLMakie/Makie may handle this more cleanly than fighting Plots.jl's somewhat awkward native pairing. The Sprint 11 Subtitle button and its (confirmed non-functional) kwarg were removed rather than shipped as dead UI. Deserves its own full SDD-level scoping conversation. |

| 15 | Heatmap plot type crashes with a GR backend error (`MethodError: no method matching size(::Nothing)`) — same root cause as Sprint 9's Area bug: `heatmap` needs a 2D Z-value matrix, not two 1D vectors, but `buildCallArgs()` falls through to the generic `seriestype=:heatmap` branch which only ever passes X/Y | Pre-existing code-gen gap, never tested until Sprint 11 | Found 2026-07-11 during Sprint 11 Task 002 testing. Same class of issue as Sprint 9's Area fix — Heatmap was never in any test table (S9-005 never covered it) and needs its own dedicated handling in `buildCallArgs()`, not the generic fallback. Also caused a cascading `"print device already activated"` GR-backend error on the NEXT plot call in the same session — worth knowing that a crashed plot call can leave the backend in a bad state requiring a fresh REPL to recover from, not something JuliaLabApp's code controls. Needs its own design pass (what does 2D data even mean for this app's variable-selection model — does Heatmap need a 3rd "Z" variable that's actually a matrix, not a vector, differently from Surface/Contour's Z?) rather than a quick fix. |

| 16 | Wolfram kernel launch shows two "failed to connect" error toasts before a final success message — looks like a timing race on cold start | Minor, likely third-party extension behavior | Found 2026-07-13 during Sprint 12 Task 003 testing. Kernel does eventually connect successfully; not investigated further — possibly inherent to `wolfbook.launchKernel`'s own retry behavior, not something our thin wrapper command could itself cause. Worth a closer look in a future sprint if it proves consistently annoying, not blocking Sprint 12. |

| 17 | Claude Code's side panel renders completely blank — no visible input field, nothing clickable, `Claude Code: Focus Input` command does nothing | Third-party extension issue, likely NOT fixable in JuliaLabApp's code | Found 2026-07-13. Extension's own logs (`Claude Code: Show Logs`) show healthy state — clean auth, MCP server running, no errors — ruling out a crash. Panel placement itself is confirmed correct (right side panel, matches user's stated preference). Strong symptom match to Sprint 9's own webview bug (blank panel, nothing interactive, no console access to diagnose) — plausible same root cause: `codium serve-web`'s browser-hosted webview panels depend on a fragile service-worker mechanism for loading EXTERNAL script resources, which failed silently for JuliaLabApp's own Plot Builder webview until fixed by inlining the script directly into the HTML (Sprint 9). If Claude Code's panel loads its interactive content via a similar external-script pattern, it would hit the identical platform-level failure — but this is UNVERIFIED, since Claude Code's own webview implementation isn't something we can inspect or fix. Not JuliaLabApp code to patch; worth reporting upstream to Anthropic if confirmed, or revisiting if julia-vscode/VSCodium/Electron versions change. |

| 18 | julia.exe REPL processes accumulate across `npm start` restarts and aren't cleaned up on app close — 3 orphaned bare `julia.exe` processes found during Sprint 12 teardown testing (2026-07-13), confirmed via `CommandLine` inspection to be julia-vscode's own REPL launches (no arguments), NOT Pluto-spawned processes (which always include `-e "using Pluto; ..."` in their command line — none of the three showed this) | Pre-existing teardown gap, NOT a Sprint 12 regression | Never previously checked for — every prior sprint's teardown verification only matched `codium-tunnel`/`serve-web`, never `julia`. Sprint 12's checklist was the first to widen the pattern (specifically because Pluto introduced Julia subprocess spawning), which is what surfaced this — but the actual leaking process is julia-vscode's own REPL, unrelated to anything Sprint 12 built. Real resource leak worth fixing eventually (each `npm start` restart during a long dev session leaves another orphaned REPL). |

| 19 | Plot Variables (Sprint 10) doesn't refresh for variables defined AFTER the panel is already visible — only refreshes on `onDidChangeVisibility` (toggling closed→open) plus once at activation, so if the panel stays open while new variables are typed in the REPL, they never appear until manually toggled | Pre-existing Sprint 10 design gap, not a Sprint 12 regression | Surfaced during Sprint 12 teardown testing (2026-07-13) — John reported Plot Variables not showing newly-defined variables. Root cause is the refresh-trigger design itself (DESIGN-sprint10 §4.2 flagged this as "not fully resolved" at the time, deferred as acceptable). Related, separate finding: clicking the Julia Activity Bar icon at all (for any reason) triggers Plot Variables' visibility-change refresh since it shares the `julia-explorer` container with the plain Workspace view — meaning the REPL visibly runs the variable-dump code on every such click, even when the user just wanted to look at Workspace, not Plot Variables specifically. Both point at the same underlying fix: a more targeted refresh trigger (e.g. on REPL output/execution, or a manual refresh button) rather than container-visibility-based refresh. |

| 20 | Several Lean4 menu items open a pane but aren't interactive — "Show Documentation Resources" opens Simple Browser (blank/non-functional, consistent with backlog #7's already-logged Simple Browser constraint), "Show Unicode Input Abbreviations" splits the editor with no visible abbreviations, "Search With Loogle" splits the editor with no way to actually interact with it. Also: Julia "About" button (HOME/HELP group) is non-functional | Mix of pre-existing gaps and third-party extension behavior, NOT Sprint 12 regressions | Found during Sprint 12 final teardown testing (2026-07-13). Task 003 already confirmed the Lean QuickPick correctly dispatches the right command IDs — these are the RECEIVING extension's own rendering/interaction behavior once the correct command fires, not a dispatch bug on our side. Likely the same class of issue as Claude Code's blank panel (backlog #17) — third-party webview content not rendering/interacting correctly in this app's `serve-web` context. "About" button was never tested before (same category as backlog #7's other never-verified Sprint 8 buttons). None of this is Sprint 12 scope to fix. |

None of these block Sprint 9's remaining tasks (007, 008, 011) or the
still-separately-flagged 009/010. Revisit at Sprint 10 planning.
