# Backlog — logged during Sprint 9 (2026-07-10/11), unscheduled

| # | Item | Category | Notes |
|---|---|---|---|
| 1 | FIGURES tab selection buttons cut off at bottom of ribbon | UI/layout bug | Likely related to ADR-021's fixed `RIBBON_HEIGHT=124` SSOT — FIGURES tab may have more content rows than HOME/CODE/VIEW accommodate at that height. Needs investigation, not a quick fix — could require either increasing ribbon height (affects all tabs) or restructuring FIGURES layout (affects only this tab). Undesigned. |
| 2 | Plot Builder webview panel should auto-close on JuliaLab shutdown | Teardown/lifecycle | Currently no explicit disposal hook tied to app quit — relies on default VS Code panel disposal behavior, unverified whether that's clean or leaves state. Should be evaluated alongside existing teardown verification discipline (PowerShell process-diff technique) rather than assumed fixed by a small code change. |
| 3 | Command Palette (`Ctrl+Shift+P`) is high-friction for users coming from MATLAB — no lower-friction path to it currently | UX/discoverability | Options to consider later: a ribbon button, a persistent search-style affordance, or exposing frequently-needed commands directly on relevant ribbon tabs instead of requiring the palette at all. Genuinely open design question, not a quick win — flagging rather than guessing at a fix. |
| 4 | F2 (Hide Ribbon) reverts to standard VSCodium look — asking whether this is desired/reversible behavior | Behavior question, not clearly a bug | Sounds like F2 is working as designed (SPRINT9-HANDOFF.md lists "F2 / Hide Ribbon: ✓ Collapses to tab strip") — worth clarifying with John whether the actual complaint is that it *fully* reverts to standard VSCodium chrome rather than just collapsing to tab strip, which would be a scope difference from what Sprint 6-8 shipped. Needs clarification before it's actionable. |
| 5 | Activity bar icons are monochrome line drawings — request to colorize (example: Wolfbook icon, referenced against a colorful stylized wolf-head logo) | Visual/branding polish | Undesigned. Would need icon assets sourced/created per activity-bar entry (Pluto, Lean, Wolfram, Claude, Julia, etc.) — also adjacent to Task 009 (activity bar launchers), which is itself already flagged as not-yet-designed in tasks.md. Worth designing together rather than separately, when that task is picked up. |

| 6 | Plot Builder interaction model — variable selection resets every time the panel is reopened/revealed (re-clicking Plot after adjusting a ribbon option wipes X/Y dropdown selections), and John's broader critique: selecting plot type/style/axes on the ribbon THEN variables in a separate webview is clumsy — worth exploring "pick variables first from workspace, plot options after" or a single unified surface | UX/interaction design | Root mechanical cause of the reset identified during Task 007 testing: `openPlotBuilderPanel()` re-sends `init` and re-populates dropdowns from scratch on every Plot-button click, even when revealing an already-open panel, discarding in-progress selections. That's a real, fixable bug regardless of the larger design question. The larger question (should the two-context ribbon+webview model change entirely) needs real Socratic design work — not a quick patch — deferred to a future sprint per John's explicit request (2026-07-11) to prove Task 007's core execution path first. |

| 7 | Four Sprint 8 "✓ Wired" features found non-functional or worse during Sprint 9 regression testing (2026-07-11): New Folder (no effect), Go to Def ("No Definition Found"), Forward/Back (no action), and Instantiate (spawns multiple Julia REPL terminals that immediately show "Lost connection to the editor") | Pre-existing gap, confirmed NOT a Sprint 9 regression | `ws-dispatch-probe.txt` (Task 002's diagnostic, still active) confirmed `language-julia.instantiateEnvironment` reaches the extension host correctly with the right message shape — ruling out Sprint 9's ADR-024/main.js dispatch changes as the cause. John confirmed he doesn't recall these ever having been behaviorally verified in Sprint 6-8 — "✓ Wired" apparently meant "WS message sends," not "resulting behavior confirmed." The Instantiate REPL-spawn-storm is the highest-priority of the four — real process/state instability, not just an inert button — worth its own investigation in a future sprint. |

None of these block Sprint 9's remaining tasks (007, 008, 011) or the
still-separately-flagged 009/010. Revisit at Sprint 10 planning.
